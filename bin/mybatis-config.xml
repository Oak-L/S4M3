<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 引入properties -->
	<properties resource="db.properties">
		<!-- 在properties中还可以配置一些属性名和属性值 -->
		<!-- <property name="" value=""/> -->
	</properties>
	<!-- 设置 -->
	<settings>
		<!-- 指定log4j2记录日志 -->
		<setting name="logImpl" value="LOG4J2" />
		<!-- 打开延迟加载的开关 -->
		<setting name="lazyLoadingEnabled" value="true" />
		<!-- 将积极加载改为消极加载(按需加载) -->
		<setting name="aggressiveLazyLoading" value="false" />
	</settings>
	<!-- 别名定义 -->
	<typeAliases>
		<!-- 针对单个别名定义 -->
		<!-- type:类型的路径,alias:别名 -->
		<!-- <typeAlias type="com.oracle.entity.User" alias="user" /> -->
		<!-- 批量别名定义 -->
		<!-- 指定包名:mybatis自动扫描包中的po类,自动定义别名,别名就是类名(首字母大小写都可以) -->
		<package name="com.oracle.entity" />
	</typeAliases>
	<!-- 和spring整合后用c3p0代替 -->
	<environments default="development">
		<environment id="development">
			<!-- jdbc事务管理，事务控制用mysql -->
			<transactionManager type="JDBC" />
			<!-- 数据库连接池 -->
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments>
	<!-- 加载映射文件 -->
	<mappers>
		<!-- resource:一次只加载一个映射文件 -->
		<!-- <mapper resource="com/oracle/entity/UserMapper.xml" /> -->
		<!-- <mapper resource="com/oracle/mapper/UserMapper.xml" /> -->
		<!-- class:通过mapper接口加载映射文件,需要遵循一些规范 -->
		<!-- 需要将mapper接口类名和mapper.xml映射文件名称保持一致，且在一个目录 -->
		<!-- 前提是使用的mapper代理方法 -->
		<!-- <mapper class="com.oracle.mapper.UserMapper" /> -->
		<!-- 批量加载mapper:指定mapper接口的包名,mybatis自动扫描包下所有mapper接口进行加载 -->
		<!-- 需要将mapper接口类名和mapper.xml映射文件名称保持一致，且在一个目录 -->
		<!-- 前提是使用的mapper代理方法 -->
		<package name="com.oracle.mapper" />
	</mappers>
</configuration>